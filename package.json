{
  "name": "taha-mern-portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "dev": "nodemon server/index.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}